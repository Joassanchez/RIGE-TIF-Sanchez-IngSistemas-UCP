# ADR-027 — Flujo de valor seleccionado: consulta del estado efectivo de un agente (RF-01 y RF-02), con criterio fijado antes de medir

- Estado: aceptado (retroactivo)
- Fecha: AE2 (septiembre de 2026)
- Capítulos afectados: Cap. IV (IV.3), Cap. V
- Origen: Cap. IV, IV.3 (Tabla 13)

### Contexto
Decisión tomada durante la elaboración de la AE2 y reconstruida en la migración del repositorio.

### Alternativas evaluadas
- **Adoptada:** Seleccionar como flujo automatizable la consulta del estado efectivo de un agente y fijar antes de la medición qué resultado de la línea de base confirma, reordena o refuta la selección.
- **Descartada:** Justificar la selección después de conocer los datos.

### Análisis (trade-offs)
La exigencia (vi) pide cotejar el flujo con la línea de base, que todavía no se mide; fijar el criterio antes impide ajustarlo a los datos.

### Recomendación y fundamento
La exigencia (vi) pide cotejar el flujo con la línea de base, que todavía no se mide; fijar el criterio antes impide ajustarlo a los datos.

### Decisión del autor
Seleccionar como flujo automatizable la consulta del estado efectivo de un agente y fijar antes de la medición qué resultado de la línea de base confirma, reordena o refuta la selección.

### Consecuencias
Se refleja en: Cap. IV (IV.3), Cap. V.

### Evidencia
Cap. IV, IV.3 (Tabla 13).
